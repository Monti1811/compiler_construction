char *Format;
  
int printf(char *, char*);

int main(int argc, char **argv) {
    Format = "%s\n";

    int i;
    i = 1;

    while (i < argc) {
        char* text;
        text = argv[i];
        printf(Format, text);
        printf(Format, argv[i]);
        i = i + 1;
    }

    return 10;
}