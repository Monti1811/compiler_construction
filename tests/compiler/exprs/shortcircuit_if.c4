int main(void) {
    int a;
    int b;
    int r;

    a = 0;
    b = 1;
    r = 0;

    if (a && (r = r + 1)) {
        r = r + 2;
    }
    if (b || (r = r + 4)) {
        // ok
    }
    if (a ? (r = r + 5) : 0) {
        r = r + 16;
    }
    if (b ? 1 : (r = r + 32)) {
        // ok
    }

    return r;
}
