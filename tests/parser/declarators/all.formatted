int (main())
{
	int (f(int a, char b));
	int (g(int, char));
	int (h(int (lambda(int x))));
	int (i(int (lambda(int))));
	int (j(int(int x)));
	int (k(int(int)));
	int (*a);
	int (*(*b));
	int (*);
	int (*(*));
	int c;
	int;
}
